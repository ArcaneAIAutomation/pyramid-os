{
  "name": "@pyramid-os/society-engine",
  "version": "0.1.0",
  "description": "Planning and scheduling engine for PYRAMID OS — task queues, resources, zones, and build phases",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pyramid-os/blueprint": "workspace:*",
    "@pyramid-os/data-layer": "workspace:*",
    "@pyramid-os/logger": "workspace:*",
    "@pyramid-os/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "better-sqlite3": "^11.10.0",
    "fast-check": "^3.23.2",
    "rimraf": "^6.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
